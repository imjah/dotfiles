general {
	colors         = true
	interval       = 2
	color_good     = "#ebdbb2"
	color_degraded = "#d79921"
	color_bad      = "#cc241d"
}

order += "ethernet _first_"
order += "wireless _first_"
order += "battery all"
order += "disk /mnt/passwords"
order += "disk /mnt/files"
order += "disk /"
order += "memory"
order += "cpu_usage"
order += "volume master"
order += "time"

ethernet _first_ {
	format_up = "󰈀 %speed 󰩟 %ip"
	format_down = ""
}

wireless _first_ {
	format_up = "󰖩 %essid %bitrate 󰩟 %ip"
	format_down = ""
}

battery all {
	format = "%status %percentage 󱧥 %remaining"
	format_down = ""
	format_percentage = "%.00f%s"
	status_chr = "󰂄"
	status_bat = "󰁹"
	status_full = "󰚥"
	low_threshold = 20
}

disk /mnt/passwords {
	format = "󰟵 %used"
}

disk /mnt/files {
	format = "󰆼 %used"
}

disk / {
	format = "󰋊 %used"
}

memory {
	format = "󰍛 %used"
}

cpu_usage {
	format = "󰻠 %usage"
}

volume master {
	format = "󰕾 %volume"
	format_muted = "󰝟 Muted"
	mixer = "Master"
	mixer_idx = 0
}

time {
	format = "%a %d %b %H:%M"
}
